# 病名トークン curation バックログ（黙って捨てないための可視化）

`loaded.unresolvedDiseaseTokens`（`reportUnresolvedDiseaseTokens`）が、算定ルール調査DBの
病名トークンのうち**コードに解決できなかったもの**を露出する。解決できないトークンを含む
不適応ルールは**警告が発火しない**（=その分だけ効いていない）。誤検知を避けるため
fuzzy（部分一致）解決はしない方針なので、ここに挙がったものは**個別に正しいコード集合へ
curation（できれば歯科医師確認）**して有効化する。

## 現状の未解決トークン（2026-06-15 時点・11件）

歯科の略号・カテゴリ名（「破折」「脱離」等）は、傷病名マスタでは複数の具体コードに分かれる
（例: 破折 → 歯冠破折5218009 / 外傷性歯の破折8844150 …）。単一コードに一意化できないため
未解決。curation では「そのカテゴリに属するコード群」を明示して解決する。

※ 重複排除（2026-06-15）で同一審査事例の二重エントリ33件を削除済み。未解決トークンは19→11件に減少。

| ruleId | field | token | 備考（候補コード） |
|---|---|---|---|
| DP013 | forbidden | 脱離（単独） | ○○脱離 系（8843811 等）。単独脱離病名の扱い要確認 |
| DP014 | forbidden | 脱離（単独） | 同上 |
| DP025 | required | Perico+HIT水平埋伏智歯 | 複合条件（智歯周囲炎＋水平埋伏智歯8847213）。適応＝非発火 |
| DP039 | forbidden | 義歯ハソン | 義歯破損 系（9999524 等） |
| DP040 | forbidden | 鉤（Cl）ハセツ | クラスプ破折。傷病名コード要特定 |
| DP041 | required/forbidden | ハセツ / ハセツなし | 破折 系（複数コード） |
| DP065 | required | P急発 | 8843614 慢性辺縁性歯周炎急性発作 等。適応＝非発火 |
| DP070 | required | ハセツ | 破折 系。適応＝非発火 |
| DP073 | forbidden | 義歯ハセツ | 義歯破損 系 |
| DP074 | required | P急発 | 同 DP065。適応＝非発火 |

注: required（適応）トークンはアラート発火に使っていない（適応≠必須の方針）ため、
**実害があるのは forbidden（不適応＝警告）側**＝ DP013/014（脱離）・DP039（義歯ハソン）・
DP040（鉤ハセツ）・DP041（ハセツなし）・DP073（義歯ハセツ）。優先度はこの forbidden を上に。

## やり方（取込 → 検証 → 有効化）

1. 各 token を傷病名マスタ名称で検索し、該当する**コード群**を確定する（カテゴリは複数コード可）。
2. `disease_master` に略号エイリアスとして追加するか、`diagnosis_procedure` の token を
   `"略号/コード"` 形式へ具体化する。
3. 歯科医師レビュー後、`npm test` の `unresolvedDiseaseTokens` 件数が減ることを確認。
