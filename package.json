{
  "name": "sourirette",
  "version": "0.0.1",
  "private": true,
  "description": "レセコン一体型 歯科電子カルテ（フルスクラッチ）",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "test": "tsc && node --test \"dist/test/*.test.js\"",
    "uke:demo": "tsc && node dist/src/receipt/cli.js"
  },
  "engines": {
    "node": ">=22"
  },
  "devDependencies": {
    "@types/node": "^25.9.3",
    "typescript": "^6.0.3"
  }
}
